handle: samoht

storage:
  bucket: homebrew-bottles
  region: fr-par
  profile: homebrew-samoht

tap:
  clone_url: https://github.com/samoht/homebrew-tap.git
  push_url: git@github.com:samoht/homebrew-tap

build:
  linux: static
  switch: lto-5.4.1

packages:
  - name: cascade
    target: bin/main.exe
    description: "CSS formatter, minifier, inliner, and structural diff tool"
