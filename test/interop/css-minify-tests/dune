(test
 (name test)
 (libraries cascade alcotest astring fmt)
 (deps
  (source_tree traces)))

; Refresh traces against keithamus/css-minify-tests at the pinned commit.

(rule
 (alias regen-traces)
 (deps
  (source_tree scripts))
 (action
  (chdir
   scripts
   (run ./generate.sh))))
