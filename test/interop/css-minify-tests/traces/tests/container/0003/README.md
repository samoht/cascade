# Container style query whitespace

`style()` container queries must be preserved. Whitespace around `:` inside
the condition can be removed but the `style()` function itself must not be
stripped as unknown.
