# @import with media query

`@import url("foo.css") screen` shortens to `@import"foo.css"screen`. The
string form removes the url() wrapper and no space is needed between the closing
quote and the media query keyword.
