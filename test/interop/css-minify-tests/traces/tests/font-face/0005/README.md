# font-family quotes required for generic family keywords

Unquoting `"serif"` produces the generic family keyword `serif`, which refers
to the browser default serif font -- not a custom font named "serif". Quotes
must be preserved when the family name matches a generic keyword.
