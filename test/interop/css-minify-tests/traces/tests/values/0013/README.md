# Gradient function whitespace

Whitespace inside `linear-gradient()` around commas and arguments should be
stripped. Uses a numeric angle and short hex colors to avoid confounding
direction keyword or color conversions.
