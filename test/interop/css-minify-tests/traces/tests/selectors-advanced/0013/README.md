# h1-h6 selector list to :heading

`h1,h2,h3,h4,h5,h6` can be replaced with `:heading` per CSS Selectors Level 5.
The `:heading` pseudo-class matches any element with a heading level. Its
specificity is that of a class. No browser implements this yet.
