# @position-try equivalent to flip-block tactic

A `@position-try` that only flips the block axis (bottom -> top) is equivalent
to the built-in `flip-block` try tactic. The at-rule can be removed and the
reference replaced with the keyword, saving the entire block.
