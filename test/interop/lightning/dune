(test
 (name test)
 (libraries cascade alcotest unix re.str fmt)
 (modules test trace_pairs)
 (deps
  (source_tree traces)))

(executable
 (name bench)
 (libraries cascade unix fmt)
 (modules bench trace_pairs))

; Regenerate traces against Lightning CSS: dune build @regen-traces

(rule
 (alias regen-traces)
 (deps
  (source_tree scripts))
 (action
  (chdir
   scripts
   (run ./generate.sh))))
